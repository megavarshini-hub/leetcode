#include <stdio.h>

int main()
{
    int arr[]={1, 2, 3, 4, 5};
    int n= sizeof(arr) / sizeof (arr[0]);
    for (int i = n - 1; i >= 0; i--)
    {
        printf("%d ", arr[i]);
    }
    

    return 0;
}
//python
arr =[1, 2, 3, 4, 5]
    n=len(arr)
   
    for i in range(n - 1,-1,-1):
        print(arr[i],end='')
